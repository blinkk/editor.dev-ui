title: Editor.dev API documentation
navTitle: API Docs
description: Documentation for the api for developing for the editor.

partials:
- partial: spacer
  options:
  - large

- partial: hero
  title: Editor.dev API
  body: |
    The API has two main parts: the UI for providing a rich editor and the
    Connector that connects the editor to the project source code.
  buttons:
  - label: UI API
    url: /api/ui/
  - label: Connector API
    url: /api/connector/

- partial: spacer
  options:
  - large
